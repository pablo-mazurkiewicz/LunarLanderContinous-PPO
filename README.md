To train models again: "python filename.py seednumber"
To see the landings: "python watch_ppo.py" -> make sure to specify the correct paths.
To evaluate model: "python eval.py" -> make sure to specify the correct paths and enviromental variables

Training log files are getting saved at C:/rl_logs by force. To see the graphs enter "tensorboard --logdir=C:/rl_logs" and follow the link.
